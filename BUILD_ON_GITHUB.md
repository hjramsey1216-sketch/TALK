# TALK Android build

This project includes a GitHub Actions workflow at `.github/workflows/build.yml`.
After the project files are in the repository, open GitHub Actions and run
**Build TALK APK**. The resulting debug APK is uploaded as an artifact.
